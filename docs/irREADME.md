## functions
###    *add [loc | var] [int_lit | var | loc] [int_lit | var | loc]
        Adds the last 2 args and saves in the first.

###    *sub [loc | var] [int_lit | var | loc] [int_lit | var | loc]
        Subtracts the last 2 args and saves in the first.

###    *mult [loc | var] [int_lit | var | loc] [int_lit | var | loc]
        Multiplies the last 2 args and saves in the first.

###    *div [loc | var] [int_lit | var | loc] [int_lit | var | loc]
        Adds the last 2 args and saves in the first.

###    *printf [int_lit | char_lit | string_lit | ptr | loc]
        Prints the 1st argument with a \n.
    
###    *inline [string_lit]
        Writes the text entered into the generated C file.
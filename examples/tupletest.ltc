define customAdd(i32 x, i32 y) {
    let tuple result = makeTuple(i32, ptr);
    result[0] = x + y;
    result[1] = &result[0];
    return(result);
}

define main() {
    let i32 a = 5;
    let i32 b = 10;
    let tuple ab_result = customAdd(a, b);
    printf(ab_result);
    return(0);
}
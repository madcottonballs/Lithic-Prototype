define main() {
    let tuple x = makeTuple(i32, i8, boolean);
    /*x[0] = 1; eventual syntax*/   
    tSet(x, 0, 50);
    tSet(x, 1, 127 -> i8);
    tSet(x, 2, true);
    printf(x);
    coredump(); 
    
    
    return(0);
}
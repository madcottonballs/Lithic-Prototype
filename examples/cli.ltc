define main() {
    let i32 x = cmd("echo Hello, World!");
    let ptr[5] px;
    px[0] = &x;
    @(px[0], i32, 0) = 5;
    printf(concat("The exit code of the command was: ", x -> string));
    printf(concat("The value at px[0] is: ", @(px[0], i32, 0) -> string));
    return(0);
}
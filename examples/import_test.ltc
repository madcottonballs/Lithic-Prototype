import "dictlib.ltc";

define main() {
    /*let ptr test = dictlib.makeDict();*/
    let ptr x = malloc(5);
    tag(x, u8);
    x[0] = 10;
    iterate(i, 5) {
        printf(x[i]);
    }
    return(0);
}
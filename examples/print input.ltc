let string user_input = input();

while (user_input != "close") {
    let i32 i = 0;

    while (i < sLength(user_input)) {
        printf(user_input[i]);
        i++;
    }
    let string user_input = input();
}

** identified bug: line 10 will not work with pure assignment, only initalization. unindentended behavior

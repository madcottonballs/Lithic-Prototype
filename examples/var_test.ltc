define main() { 
    let ptr p = mallocType(u8, 100);
    tag(p, u8);

    iterate (i, 100) {
        p[i] = i;
    }
    iterate (i, 100) {
        printf(p[i]);
    }
    coredump();
    return(0);
}
define test() {
    let ptr h = malloc(10);
    return(h);    
}
define main() { 
    printf("expected output: 6, Hello, world!, True, A, 5, <ptr as num>, <ptr as num>");

    let i32 x = 6;
    printf(x);

    let string str = "Hello, world!";
    printf(str);

    let boolean b = true;
    printf(b);

    let char c = 'A';
    printf(c);

    let ptr p = malloc(5);
    printf(p);
    @(p, u8) = 5;
    printf(@(p, u8));
    
    let ptr g = test();
    printf(g);
    
    let string temp = "";
    while (temp != "exit") {
        temp = input();
        coredump();
    }
    let i32 yy = 10;
    coredump();
    printf(temp);
    return(0);
}
import "strarraylib.ltc" as "strarr";
struct pos {
    let i32 x;
    let i32 y;
}
define main() {
    let ptr[10] test;
    strarr.load("hello", 0, &test);

    let file f = open("test.txt", "r");

    while (not atEOF(f)) {
        printf(readByte(f));
    }
    return(0);
}
/* read: read a string from a pointer to a string */
define read(ptr original) {
    tag(original, char);   /* lets us do ptr indexing */
    let string result;
    let i32 i2;
    while (original[i2] != '\0') {
        result = concat(result, original[i2]);
        i2 += 1;
    }
    return(result);
}


/*
The dictlib uses a memory structure like this:
[u8 size][ptr to key][ptr to value][ptr to key][ptr to value]...
*/

define makeDict() {
    let ptr dict = malloc(1);
    @(dict) = 0;
    return(dict);
}